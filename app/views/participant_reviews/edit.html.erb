<h1>Edit Review</h1>

<%= form_for([@booking, @participant_review]) do |f| %>
  <% if @participant_review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@participant_review.errors.count, "error") %> prohibited this participant_review from being saved:</h2>
      <ul>
        <% @participant_review.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :activity_rating %>
    <%= f.number_field :activity_rating, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :comment %>
    <%= f.text_area :content, class: "form-control" %>
  </div>

  <%= f.submit "Update Review", class: "btn btn-primary" %>
<% end %>

<%= link_to 'Cancel', booking_participant_review_path(@booking, @participant_review) %>
